apiVersion: v1
kind: Service
metadata:
  name: auth-api-service
  namespace: default
  labels:
    name: auth-api-service
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - port: 80
      targetPort: 80